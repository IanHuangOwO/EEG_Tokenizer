{
  "checkpoint": "output/mesae_v10_all_share/checkpoint/last.pth",
  "dataset_params": {
    "pretrain": {
      "Nakanishi2015": { "dataset_path": "datas/finetune/Nakanishi2015", "subject_to_use": ["all"], "channels_to_use": ["all"] },
      "BETA_4s": { "dataset_path": "datas/pretrain/BETA_4s", "subject_to_use": ["all"], "channels_to_use": ["all"] },
      "BETA_3s": { "dataset_path": "datas/pretrain/BETA_3s", "subject_to_use": ["all"], "channels_to_use": ["all"] },
      "BCICIV1_Train": { "dataset_path": "datas/pretrain/BCICIV1_Train", "subject_to_use": ["all"], "channels_to_use": ["all"] },
      "BCICIV1_Test": { "dataset_path": "datas/pretrain/BCICIV1_Test", "subject_to_use": ["all"], "channels_to_use": ["all"] },
      "BNCI2014001": { "dataset_path": "datas/finetune/BNCI2014001", "subject_to_use": ["all"], "channels_to_use": ["all"] },
      "BNCI2014004": { "dataset_path": "datas/finetune/BNCI2014004", "subject_to_use": ["all"], "channels_to_use": ["all"] },
      "GraspAndLift_Train": { "dataset_path": "datas/pretrain/GraspAndLift_Train", "subject_to_use": ["all"], "channels_to_use": ["all"] },
      "GraspAndLift_Test": { "dataset_path": "datas/pretrain/GraspAndLift_Test", "subject_to_use": ["all"], "channels_to_use": ["all"] },
      "Inria_Train": { "dataset_path": "datas/pretrain/Inria_Train", "subject_to_use": ["all"], "channels_to_use": ["all"] },
      "Inria_Test": { "dataset_path": "datas/pretrain/Inria_Test", "subject_to_use": ["all"], "channels_to_use": ["all"] },
      "PhysionetMI": { "dataset_path": "datas/pretrain/PhysionetMI", "subject_to_use": ["all"], "channels_to_use": ["all"] }
    }
  },
  "check": {
    "analysis": "both",
    "plot_recon": true,
    "plot_topo_psd": true,
    "plot_attn_topo": true,
    "cmap": "YlOrRd",
    "codebook": {
      "_comment": "Now also caps how much gets LOADED, not just how much check_codebook samples afterward -- analysis_pretrain.py's _cap_subjects_by_trial_budget shuffles each dataset's subject list and stops adding subjects once their cumulative trial count crosses this, so subject_to_use=['all'] on e.g. PhysionetMI (109 subjects) no longer loads every real trial from every subject just to discard most of them post-hoc (that OOM'd a run at ~55GB RSS -- see docs/model-analysis-checklist.md). 3000 is generous (15x the old 200) while keeping worst-case per-dataset memory in the hundreds of MB, not GB.",
      "max_trials_per_dataset": 3000
    }
  }
}