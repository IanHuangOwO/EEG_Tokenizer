from IO.loader import MoabbLoader


class Loader(MoabbLoader):
    """Lee2019_MI through MOABB: both sessions, train and test runs (all labelled),
    one trial per left/right-hand cue. See IO/loader.py."""
