{
  "compile_params": {
    "_comment": "sample_freq/bandpass_filter/pre_event_seconds/post_event_seconds here MUST match config.json's preprocess_params — that's how the compiled cache filename is derived on both the write side (this script) and the read side (IO/dataset.py). A mismatch just misses the cache with a clear FileNotFoundError, never silently reads stale data.",
    "sample_freq": 200,
    "bandpass_filter": { "l_freq": 0.5, "h_freq": 100.0 },
    "pre_event_seconds": 1.0,
    "post_event_seconds": 4.0,
    "_pre_post_comment": "Global — injected into every loader's dataset_params uniformly (see cache_dataset.py), same mechanism dataset_path already uses. Only the trigger/annotation-anchored loaders read them (BCICIV1_Train, BCICIV2a, BCICIV2b, Inria_Train); everyone else's loader ignores them (see e.g. datas/EEGMMIdb/loader.py, which deliberately opts out even though it receives them — real headroom there is ~0, see docs/model-analysis-checklist.md). Total window = pre+post = 5.0s = 1000 samples @ 200Hz, must match config.json's preprocess_params.window_length.",
    "datasets": {
      "Dial": { "dataset_path": "datas/Dial" },
      "BETA_4s": { "dataset_path": "datas/BETA_4s" },
      "BETA_3s": { "dataset_path": "datas/BETA_3s" },
      "BCICIV1_Train": { "dataset_path": "datas/BCICIV1_Train" },
      "BCICIV1_Test": { "dataset_path": "datas/BCICIV1_Test" },
      "BCICIV2a": { "dataset_path": "datas/BCICIV2a" },
      "BCICIV2b": { "dataset_path": "datas/BCICIV2b" },
      "Inria_Train": { "dataset_path": "datas/Inria_Train" },
      "Inria_Test": { "dataset_path": "datas/Inria_Test" },
      "EEGMMIdb": { "dataset_path": "datas/EEGMMIdb" },
      "GraspAndLift_Train": { "dataset_path": "datas/GraspAndLift_Train" },
      "GraspAndLift_Test": { "dataset_path": "datas/GraspAndLift_Test" },
      "DEAP": { "dataset_path": "datas/DEAP" }
    }
  }
}
